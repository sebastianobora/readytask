import { FocusOnChangeDirective } from './focus-on-change.directive';

describe('FocusOnChangeDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new FocusOnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
